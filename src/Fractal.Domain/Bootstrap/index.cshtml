<!DOCTYPE html>
<html lang="en" ng-app >
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <script src="~/Scripts/angular.js"></script>
</head>
<body>
    <h1>EMIDIUM{{'HORAY' + '!'}}</h1>
    <li ng-repeat="dc in dcs">
        <span>{{dc.id}}</span>
        <p>{{dc.name}}</p>
    </li>
</body>
</html>
